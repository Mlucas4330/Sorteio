version: '3'

services:
  api:
    environment:
      - NODE_ENV: development
      - DB_URL: postgres://${DB_USER}:${DB_PASS}@db:5432/${DEV_DB_NAME}
  db:
    environment:
      - DB_NAME: ${DEV_DB_NAME}
