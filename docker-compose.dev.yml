version: '3'

services:
  db:
    environment:
      POSTGRES_DB: ${DEV_DB_NAME}
  api:
    environment:
      NODE_ENV: development
      DB_URL: postgres://${DB_USER}:${DB_PASS}@db:5432/${DEV_DB_NAME}
