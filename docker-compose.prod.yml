version: '3'

services:
  api:
    environment:
      NODE_ENV: production
      DB_URL: postgres://${DB_USER}:${DB_PASS}@db:5432/${PROD_DB_NAME}
  db:
    environment:
      DB_NAME: ${PROD_DB_NAME}
