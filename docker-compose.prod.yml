version: '3'

services:
  db:
    environment:
      POSTGRES_DB: ${PROD_DB_NAME}
  api:
    environment:
      NODE_ENV: production
      DB_URL: postgres://${DB_USER}:${DB_PASS}@db:5432/${PROD_DB_NAME}
